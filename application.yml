
management:
  endpoints:
    web:
      exposure:
        include: "refresh, env"

---
spring:
  profiles: imac
  
logging:
  level:
    openmart:
      apiserver:
        api: DEBUG
  path: "/Users/sungjun.kim/tank0/logs"
  
---
spring:
  profiles: macbook

logging:
  level:
    reactor:
      netty:
        http:
          client:
            HttpClient: DEBUG
    openmart:
      apiserver:
        api: DEBUG
  path: "/tank0/logs"
  
---
spring:
  profiles: stg,prd

logging:
  level:
    openmart:
      apiserver:
        api: ERROR
  path: "/home/ec2-user/tank0/logs"
